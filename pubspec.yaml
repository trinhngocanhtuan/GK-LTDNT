name: gk_ltdd
description: A new Flutter project.

publish_to: 'none' # Prevent accidental publishing to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  # Firebase dependencies
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4
  firebase_auth: ^5.3.1 # Nếu bạn cần dùng Firebase Auth
  firebase_storage: ^12.3.3 # Nếu bạn cần dùng Firebase Storage
  firebase_database: ^11.1.0 # Đã thêm firebase_database

  # Image picker
  image_picker: ^1.0.0

  # Cupertino Icons for iOS style icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
